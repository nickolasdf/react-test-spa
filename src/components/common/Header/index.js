import React from 'react';
import Timer from './Timer';

import './index.scss';

const Header = () => {
    return (
        <header className="header">
            <Timer/>
        </header>
    );
};

export default Header;
